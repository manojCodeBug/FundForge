[workspace]
resolver = "2"
members = [
    "contracts/campaign-registry",
    "contracts/funding-escrow",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
strip = true
lto = true
codegen-units = 1
panic = "abort"
